{
	"hydrolix": {
		"url": "http://my.hydrolix.instance",
		"project": "hdx_demo",
		"logs_table": "hdb_logs",
		"logs_transform": "hdb_logs_transform",
		"analytics_table": "required if includeSystemInfo = true",
		"analytics_transform": "required if includeSystemInfo = true"
	}
}
